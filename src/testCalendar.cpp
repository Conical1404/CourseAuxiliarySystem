#include "Calendar.hpp"
#include "String.hpp"
#include "Vector.hpp"
#include "AVL.hpp"

// using namespace std;

int main() {
    // AVL<int> tr;
    // tr.show_AVL(*tr.getRoot());
    // std::cout << std::endl;
    // tr.insert(tr.getRoot(), 1);
    // tr.show_AVL(*tr.getRoot());
    // std::cout << std::endl;
    // tr.Delete(tr.getRoot(), 1);
    // tr.show_AVL(*tr.getRoot());
    // std::cout << std::endl;
    // for(int i = 1; i < 7; i ++) {
    //     tr.insert(tr.getRoot(), i);
    //     tr.show_AVL(*tr.getRoot());
    //     std::cout << std::endl;
    // }
    // for(int i = 1; i < 7; i ++) {
    //     tr.Delete(tr.getRoot(), i);
    //     tr.show_AVL(*tr.getRoot());
    //     std::cout << std::endl;
    // }
    return 0;
}
